fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy>=2.0
alembic>=1.13
python-dotenv>=1.0

# Database: SQLite is used by default and ships with Python — no driver needed.
# To run against PostgreSQL instead, set DATABASE_URL to a postgresql:// URL
# and install the driver:  pip install "psycopg[binary]>=3.1"
