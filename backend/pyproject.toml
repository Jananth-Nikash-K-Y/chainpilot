[project]
name = "chainpilot-backend"
version = "0.1.0"
description = "ChainPilot - Agentic AI Supply Chain Control Tower (backend)"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
