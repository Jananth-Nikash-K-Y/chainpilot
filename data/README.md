# Data

Local, non-production data artifacts.

- `raw/`       — unprocessed source data
- `processed/` — cleaned/derived data
- `sample/`    — small sample datasets for development and demos

Nothing is tracked here yet beyond directory placeholders.
